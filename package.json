{
  "version": "0.5.0",
  "name": "express-dump-curl",
  "description": "ExpressJS middleware to dump received requests as curls and RESTests",
  "keywords": [
    "express",
    "curl",
    "dump",
    "debug"
  ],
  "main": "index.js",
  "repository": "git@github.com:fsoft72/express-dump-curl.git",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^12.12.7"
  },
  "scripts": {
	  "build": "tsc --build",
	  "dev": "tsc --build -w"
  }
}
